import 'package:flutter/material.dart';

class DependenciesManger extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Dependencies Manger'),
      ),
      body: _buildScreenWidgets(),
    );
  }

  Widget _buildScreenWidgets() {
    return Container();
  }
}
